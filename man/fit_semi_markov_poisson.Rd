% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/semi_markov_poisson.R
\name{fit_semi_markov_poisson}
\alias{fit_semi_markov_poisson}
\title{Fit semi-Markov Poisson regressions on a time-duration grid}
\usage{
fit_semi_markov_poisson(paths, t_grid, u_grid, transitions = NULL)
}
\arguments{
\item{paths}{List of jump paths. Each element must contain `times` and
`states` vectors.}

\item{t_grid}{Strictly increasing time grid.}

\item{u_grid}{Strictly increasing duration grid.}

\item{transitions}{Optional transitions to fit. Use `"i->j"`, shorthand
`"ij"`, or a two-column matrix/data.frame with `(from, to)`.}
}
\value{
An object of class `jump_pois_semi_markov_fit` with fitted `glm`
  objects, occurrence-exposure table, and fitted box rates.
}
\description{
Fits one Poisson regression per transition with grid-box-specific effects and
offset `log(exposure)`. Intensities are piecewise constant on the
`(time, duration)` boxes induced by `t_grid` and `u_grid`.
}
